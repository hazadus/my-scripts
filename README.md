# `my-scritps` – утилиты на Python под `uv`

## Материалы по теме

- [Better alternative to shell scripts with Python, uv and pytest](https://hamatti.org/posts/better-alternative-to-shell-scripts-with-python-uv-and-pytest/)
- [Pesky little scripts](https://rednafi.com/misc/pesky_little_scripts/)

## `ytdlp.py`

### Требования

- `ffmpeg` for highest quality downloads (`brew install ffmpeg`)

```bash
# Скачать ссылку
uv run ytdlp.py "https://youtu.be/VGF8FAV1eeM?si=RU8FP4JHrsduOJCh"

# Скачать ссылку в высшем качестве (требуется ffmpeg)
uv run ytdlp.py -hq "https://youtu.be/VGF8FAV1eeM"

# Скачать ссылку из буфера
uv run ytdlp.py "$(pbpaste)"

# Скачать ссылку из буфера в высшем качестве
uv run ytdlp.py -hq "$(pbpaste)"

# Открыть папку с закачками в Finder
open "/Users/hazadus/Downloads/FromYouTube"
```
## `gitlines.py`

Скрипт для анализа коммитов репозитория GitHub, отслеживания добавленных/удалённых строк и количества файлов в каждом коммите с использованием GitHub API.

### Требования

- GitHub Token с правом чтения репозитория

```bash
# Анализ репозитория
uv run gitlines.py "https://github.com/username/repo" your_github_token
```

Выводит таблицу с SHA, датой, количеством добавленных/удалённых строк, количеством файлов и сообщением коммита. В конце — суммарная статистика.

## `ycs3.py`

Скрипт для загрузки файла в S3-совместимое хранилище Яндекс Облака.

### Требования

- Указать свои AWS_ACCESS_KEY и AWS_SECRET_KEY в скрипте
- Доступ к бакету Яндекс Облака

```bash
# Загрузить файл в S3
uv run ycs3.py /path/to/file
```

Файл будет загружен в бакет с именем, указанным в переменной AWS_BUCKET_NAME.

## `clipmon.py`

Скрипт для мониторинга буфера обмена macOS. Автоматически сохраняет новые текстовые данные и изображения (PNG) из буфера обмена в файлы с временными метками.

### Требования

- macOS (использует AppKit)
- Права доступа к буферу обмена

### Особенности

- **Мониторинг в реальном времени**: отслеживает изменения в буфере обмена каждые 5 секунд
- **Поддержка текста и изображений**: сохраняет текстовые данные (.txt) и PNG изображения (.png)
- **Автоматическая организация**: создаёт отдельные папки для текста и изображений
- **Временные метки**: все файлы сохраняются с уникальным временным штампом
- **Логирование**: подробная информация о процессе сохранения

### Использование

```bash
# Запустить мониторинг буфера обмена
uv run clipmon.py
```

Файлы сохраняются в домашней папке пользователя:
- Текстовые данные: `~/clipmon/text/clip_YYYY-MM-DD_HH-MM-SS.txt`
- PNG изображения: `~/clipmon/images/clip_YYYY-MM-DD_HH-MM-SS.png`

Для остановки мониторинга используйте `Ctrl+C`.

## `toot.py`

Скрипт для публикации сообщений в Mastodon с использованием Mastodon.py API.

### Требования

- Аккаунт на Mastodon (настроен для fosstodon.org)
- Первоначальная настройка OAuth (выполняется один раз)

### Первоначальная настройка

Для первого использования нужно выполнить OAuth аутентификацию:

```bash
# Выполнить процедуру OAuth аутентификации
uv run toot.py --auth
```

Скрипт автоматически зарегистрирует приложение и проведёт через процесс OAuth авторизации.

### Использование

```bash
# Опубликовать сообщение
uv run toot.py "Привет, Mastodon!"

# Опубликовать сообщение из буфера обмена
uv run toot.py "$(pbpaste)"

# Опубликовать многострочное сообщение
uv run toot.py "Первая строка
Вторая строка
Третья строка"

# Посмотреть справку по командам
uv run toot.py --help
```

## `masto_feed.py`

Скрипт для чтения сообщений в домашней ленте Mastodon за указанную дату с использованием Mastodon.py API.

### Особенности

- **Поддержка бустов (reblog)**: показывает содержимое исходного туита с указанием, что это буст и кто его забустил
- **Поддержка медиа-контента**: отображает изображения из постов
- **Гибкий вывод**: обычный текстовый формат или Markdown с эмодзи и ссылками
- **Пагинация**: автоматически загружает все посты за указанную дату (до 1000 постов)

### Требования

- Аккаунт на Mastodon (настроен для fosstodon.org)
- Файл `toot_usercred.secret` с токеном доступа (создается через `toot.py --auth`)

### Использование

```bash
# Показать посты за сегодня
uv run masto_feed.py

# Показать посты за указанную дату
uv run masto_feed.py 2024-08-13

# Вывести посты в формате Markdown (с эмодзи, ссылками и изображениями)
uv run masto_feed.py --markdown 2024-08-13

# Посмотреть справку по командам
uv run masto_feed.py --help
```

### Примеры вывода

**Обычный формат:**
```
14:30 @user1: Привет, мир!
15:45 @user2 забустил от @user3: Интересная статья о Python
16:00 @user4: [медиа/без текста]
```

**Markdown формат:**
```
----
**🕒 14:30 👤 @user1**
💬 Привет, мир!

----
**🔄 15:45 👤 @user2 забустил пост от @user3**
💬 Интересная статья о Python

[Открыть оригинальный пост](https://...)
```

## `bsky_feed.py`

Скрипт для чтения сообщений в домашней ленте Bluesky за указанную дату с использованием atproto API.

### Особенности

- **Поддержка репостов**: показывает содержимое исходного поста с указанием, что это репост и кто его сделал
- **Поддержка медиа-контента**: отображает изображения из постов
- **Восстановление ссылок**: автоматически заменяет сокращенные URL на полные ссылки
- **Гибкий вывод**: обычный текстовый формат или Markdown с эмодзи и ссылками
- **Пагинация**: автоматически загружает все посты за указанную дату (до 1000 постов)

### Требования

- Аккаунт на Bluesky
- App Password (создается в настройках Bluesky)

### Использование

```bash
# Показать посты за сегодня
uv run bsky_feed.py --identifier "username.bsky.social" --password "your-app-password"

# Показать посты за указанную дату
uv run bsky_feed.py --identifier "username.bsky.social" --password "your-app-password" 2024-08-13

# Вывести посты в формате Markdown (с эмодзи, ссылками и изображениями)
uv run bsky_feed.py --identifier "username.bsky.social" --password "your-app-password" --markdown 2024-08-13

# Посмотреть справку по командам
uv run bsky_feed.py --help
```

📖 **Подробная документация**: [docs/bsky_feed.md](docs/bsky_feed.md)

## `rss.py`

Скрипт для работы с OPML файлами RSS лент. Позволяет просматривать структуру OPML файла и читать посты из всех лент за указанную дату.

### Требования

- OPML файл с RSS лентами (например, `feeds.opml`)

### Использование

```bash
# Показать структуру OPML файла
uv run rss.py scripts/feeds.opml

# Показать список всех RSS лент в файле
uv run rss.py --list scripts/feeds.opml

# Прочитать все посты за указанную дату (формат YYYY-MM-DD)
uv run rss.py --read 2025-08-11 scripts/feeds.opml

# Прочитать посты без системных сообщений (ошибки показываются после отчета)
uv run rss.py --read 2025-08-11 --silent scripts/feeds.opml

# Прочитать посты и вывести отчет в формате Markdown
uv run rss.py --read 2025-08-11 --markdown scripts/feeds.opml

# Комбинирование флагов: тихий режим + Markdown
uv run rss.py --read 2025-08-11 --silent --markdown scripts/feeds.opml

# Посмотреть справку по командам
uv run rss.py --help
```

Скрипт выводит посты в хронологическом порядке (от старых к новым) с заголовком, ссылкой, названием ленты и датой публикации.

Флаг `--silent` убирает системные сообщения о процессе загрузки лент, а информацию об ошибках обработки показывает после основного отчета.

Флаг `--markdown` форматирует отчет в Markdown с заголовками, ссылками и эмодзи для удобного чтения и копирования в документы.

## `parse_feed.py`

Скрипт для парсинга RSS-фидов и вывода их содержимого. Поддерживает RSS, Atom и другие XML-форматы фидов.

### Особенности

- **Поддержка различных форматов**: RSS, Atom и другие XML-форматы фидов
- **Вывод структурированной информации**: название поста, ссылка, дата/время публикации
- **Обработка ошибок**: автоматические повторные попытки, обработка таймаутов и HTTP-ошибок
- **Гибкий вывод**: обычный текстовый формат или Markdown
- **Дополнительная информация**: автор, категория, описание поста (в verbose режиме)
- **Ограничение количества постов**: возможность ограничить количество выводимых постов

### Требования

- `beautifulsoup4` - для парсинга HTML/XML
- `httpx` - для HTTP-запросов
- `lxml` - для XML-парсинга
- `feedparser` - для обработки RSS/Atom фидов

### Использование

```bash
# Базовый парсинг фида
uv run parse_feed.py https://www.macworld.com/feed

# Ограничить количество выводимых постов
uv run parse_feed.py https://www.macstories.net/feed/ --limit 5

# Вывести результат в формате Markdown
uv run parse_feed.py https://daverupert.com/atom.xml --markdown

# Показать дополнительную информацию (автор, категория, описание)
uv run parse_feed.py https://blog.cassidoo.co/rss.xml --verbose

# Комбинирование флагов: ограничение + verbose + markdown
uv run parse_feed.py https://www.stefanjudis.com/rss.xml --limit 3 --verbose --markdown

# Настройка таймаута и количества повторных попыток
uv run parse_feed.py https://jvns.ca/atom.xml --timeout 60 --retries 3

# Посмотреть справку по командам
uv run parse_feed.py --help
```

### Поддерживаемые форматы

- **RSS**: RSS 0.92, RSS 1.0, RSS 2.0
- **Atom**: Atom 1.0
- **Другие XML-форматы**: автоматическое определение типа фида

### Обработка ошибок

Скрипт автоматически обрабатывает:
- HTTP-ошибки (404, 403, 429, 500+)
- Таймауты соединения и чтения
- Ошибки подключения к серверу
- Проблемы с парсингом XML
- Автоматические повторные попытки при временных ошибках

## `timeweb_llm_chat.py`

Интерактивный чат-бот с текстовым пользовательским интерфейсом (TUI) на основе LLM от Timeweb Cloud AI.

### Особенности

- **Интерактивный TUI**: удобный текстовый интерфейс с возможностью переключения темы
- **Поддержка истории диалога**: сохраняет контекст разговора в рамках сессии
- **Горячие клавиши**: `d` для переключения между светлой и тёмной темой
- **Автопрокрутка**: автоматически прокручивает к последним сообщениям
- **Визуальное разделение**: разные стили для сообщений пользователя и бота

### Требования

- Timeweb Cloud AI агент (agent_id)

### Использование

```bash
# Запустить чат с указанным agent_id
uv run timeweb_llm_chat.py your_agent_id

# Примеры взаимодействия:
# - Введите сообщение в поле ввода и нажмите Enter
# - Нажмите 'd' для переключения темы
# - Используйте Ctrl+C для выхода
```

### Управление

- **Enter**: отправить сообщение
- **d**: переключить между светлой и тёмной темой
- **Ctrl+P**: открыть палитру команд
- **Ctrl+Q**: выйти из приложения

## `auto_crop.py`

Скрипт для автоматической обрезки PNG изображений с белым или прозрачным фоном. Удаляет пустое пространство вокруг объекта, центрируя его по центру масс для идеального позиционирования.

### Особенности

- **Поддержка прозрачного фона**: корректно обрабатывает PNG с альфа-каналом (RGBA, LA)
- **Поддержка белого фона**: автоматически определяет и удаляет белый фон (RGB 255,255,255)
- **Центрирование по центру масс**: размещает объект по визуальному центру, а не по геометрическому
- **Сохранение прозрачности**: полностью сохраняет альфа-канал при обрезке
- **Квадратная обрезка**: создает квадратное изображение с равномерными отступами
- **Автоматическое именование**: сохраняет результат с суффиксом `_cropped`

### Использование

```bash
# Обрезать изображение с белым фоном
uv run auto_crop.py image_with_white_background.png

# Обрезать изображение с прозрачным фоном
uv run auto_crop.py transparent_image.png

# Результат будет сохранен как image_name_cropped.png
```

### Примеры применения

- Подготовка иконок и логотипов для веб-сайтов
- Обрезка скриншотов с лишним пространством
- Создание превью изображений для галерей
- Автоматическая обработка изображений продуктов для интернет-магазинов

Скрипт автоматически определяет тип фона (прозрачный или белый) и применяет соответствующий алгоритм обрезки с центрированием по центру масс объекта.

## `toggltrack.py`

Скрипт для получения и отображения time entries из Toggl Track за указанный диапазон дат с группировкой по проектам и суммированием времени.

### Особенности

- **Группировка по описанию**: объединяет time entries с одинаковым описанием внутри проекта и дня
- **Суммирование времени**: автоматически суммирует время для объединенных записей
- **Дневные итоги**: показывает общее время работы за каждый день
- **Информация о проектах и клиентах**: отображает названия проектов и клиентов
- **Гибкий диапазон дат**: поддерживает указание произвольного периода

### Требования

- Toggl Track аккаунт
- API ключ Toggl Track (можно получить в настройках профиля)

### Использование

```bash
# Показать время за период по умолчанию (вчера-сегодня)
uv run toggltrack.py your_api_key

# Показать время за указанную дату
uv run toggltrack.py your_api_key --start-date 2025-09-18

# Показать время за диапазон дат
uv run toggltrack.py your_api_key --start-date 2025-09-18 --end-date 2025-09-19

# Посмотреть справку по командам
uv run toggltrack.py --help
```

Скрипт автоматически получает список проектов из рабочего пространства по умолчанию и сопоставляет их с time entries для корректного отображения названий проектов и клиентов.